# Thu May 29 2025 16:23:43



```
        输入 X 
        ↓
        上下文表征 h(x)
        ↓
潜在多元各向同性柯西因果表征 P(C|x) ~ MCauchy(μ_C(h), γ_C(h))  
        ↓
表征样本的因果表征变量 C (dim = d_c) + MCauchy噪声 ε ~ MCauchy(0, γ_ε) (dim = d_ε)
输入维度为 d_c + d_ε (构成向量 U)
        ↓  
      线性变换 (V = U * W) 
Z_1, ..., Z_{K-1}  ~ Cauchy(V_k, γ_k)
        ↓  ALR 变换
分类输出 P(Y_k=k|x) (k=1,...K)